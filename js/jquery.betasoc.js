(function($) {

	$.fn.betaSoc = function(settings) {

		// extend passed in settings with defaults defined here
		settings = jQuery.extend({
			setting_name: "value"
		}, settings);

		function one(){

		}

		function two(){

		}
	}

})(jQuery);
